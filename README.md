# Love chat (proposal)

## Run locally

```bash
npx serve .
```

Then open `http://localhost:3000` in a browser.

## Or with Python

```bash
python3 -m http.server 8080
```

Then open `http://localhost:8080`.

> Photos must be in the `photos/` folder (see `PHOTOS` config in `script.js`).
