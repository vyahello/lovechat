# Love chat

## Encrypt photos

```bash
node encrypt.js
```

## Run locally

```bash
npx serve .
```

Then open `http://localhost:3000` in a browser.


## Or with Python 

```
python3 -m http.server 8080
```

Then open `http://localhost:3000` in a browser. 

